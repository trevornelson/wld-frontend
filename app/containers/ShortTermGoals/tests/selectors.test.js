// import { fromJS } from 'immutable';
// import { makeSelectShortTermGoalsDomain } from '../selectors';

// const selector = makeSelectShortTermGoalsDomain();

describe('makeSelectShortTermGoalsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
