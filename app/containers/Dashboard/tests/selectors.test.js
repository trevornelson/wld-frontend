// import { fromJS } from 'immutable';
// import { makeSelectDashboardContainerDomain } from '../selectors';

// const selector = makeSelectDashboardContainerDomain();

describe('makeSelectDashboardContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
