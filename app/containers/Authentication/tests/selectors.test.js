// import { fromJS } from 'immutable';
// import { makeSelectAuthenticationDomain } from '../selectors';

// const selector = makeSelectAuthenticationDomain();

describe('makeSelectAuthenticationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
