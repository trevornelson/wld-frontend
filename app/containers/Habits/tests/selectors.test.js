// import { fromJS } from 'immutable';
// import { makeSelectHabitsDomain } from '../selectors';

// const selector = makeSelectHabitsDomain();

describe('makeSelectHabitsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
