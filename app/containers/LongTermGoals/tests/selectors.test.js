// import { fromJS } from 'immutable';
// import { makeSelectLongTermGoalsDomain } from '../selectors';

// const selector = makeSelectLongTermGoalsDomain();

describe('makeSelectLongTermGoalsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
