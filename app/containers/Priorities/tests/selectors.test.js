// import { fromJS } from 'immutable';
// import { makeSelectPrioritiesDomain } from '../selectors';

// const selector = makeSelectPrioritiesDomain();

describe('makeSelectPrioritiesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
