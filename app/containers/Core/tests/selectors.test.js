// import { fromJS } from 'immutable';
// import { makeSelectCoreDomain } from '../selectors';

// const selector = makeSelectCoreDomain();

describe('makeSelectCoreDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
