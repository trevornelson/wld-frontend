// import { fromJS } from 'immutable';
// import { makeSelectRelationshipsDomain } from '../selectors';

// const selector = makeSelectRelationshipsDomain();

describe('makeSelectRelationshipsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
